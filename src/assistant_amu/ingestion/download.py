"""Download corpus documents listed in ``corpus/sources.yaml`` into ``corpus/raw/``.

PRD §7.1 / Phase 1 (F1). Identifiable User-Agent, robots.txt respected,
>= 1 s delay between requests, per-URL failures logged without aborting the batch.

Not yet implemented — Phase 1.
"""

from __future__ import annotations

# TODO(Phase 1): implement `python -m assistant_amu.ingestion.download` (F1).
