"""LangChain port of the query pipeline — comparison exercise only (PRD F9).

This subpackage exists only on the ``langchain-port`` branch. It reimplements the
single-turn RAG query pipeline with LangChain LCEL, reusing the same persisted
ChromaDB collection and the same system prompt as the hand-written pipeline, to
compare the two. See the branch README, section « Ce que LangChain abstrait ».
"""
